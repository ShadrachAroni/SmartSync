import 'package:flutter/material.dart';

class AppColors {
  // Primary colors (elderly-friendly, high contrast)
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryDark = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFF64B5F6);

  // Accent
  static const Color accent = Color(0xFF4CAF50);
  static const Color accentDark = Color(0xFF388E3C);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Emergency
  static const Color emergency = Color(0xFFD32F2F);
  static const Color emergencyLight = Color(0xFFFF5252);

  // Sensor values
  static const Color temperature = Color(0xFFFF6B6B);
  static const Color humidity = Color(0xFF4ECDC4);
  static const Color motion = Color(0xFFFFE66D);
  static const Color distance = Color(0xFFA8E6CF);

  // Backgrounds
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text colors (high contrast)
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textDisabled = Color(0xFFBDBDBD);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Dividers
  static const Color divider = Color(0xFFE0E0E0);

  // Disabled
  static const Color disabled = Color(0xFFE0E0E0);
}
